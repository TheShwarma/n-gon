const levelEditor = {
    createLevel(name) {
        console.log(name);
        simulation.start();
        simulation.testing = true;
    },
    editLevel(name) {
        console.log(name);
    }
};